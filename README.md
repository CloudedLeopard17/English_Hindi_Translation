# English_Hindi_Translation
Hindi Neural Machine Translation system from scratch using the Transformer architecture. Trained on the IITB parallel corpus (1.49M sentence pairs) with a custom SentencePiece BPE tokenizer. The project covers the full ML pipeline — data preprocessing and tokenizer training, model training and inference.
